.field
    .text-center
        .create_project_form
            = form_for @task, url: project_tasks_path(@task.project_id), method: :post do |form|
                = form.text_field :title, { placeholder: 'Task title' }
                %p
                = form.submit 'Add task', class: "btn-primary btn btnall"
